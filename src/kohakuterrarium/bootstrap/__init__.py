"""
Bootstrap factories for agent component initialization.

Each module contains a focused factory function that creates one
agent subsystem from an AgentConfig, reducing the import fan-out
of agent_init.py.
"""
